import { useState } from 'react'
import './index.css'

function App() {
  const [count, setCount] = useState(0)

  return (
    <div>
      {/* Navigation */}
      <nav className="nav">
        <div className="container nav-container">
          <div className="nav-brand">{{ project_title }}</div>
          <ul className="nav-links">
            <li><a href="#home" className="nav-link active">Home</a></li>
            <li><a href="#about" className="nav-link">About</a></li>
            <li><a href="#contact" className="nav-link">Contact</a></li>
          </ul>
        </div>
      </nav>

      {/* Hero Section */}
      <section className="hero">
        <div className="container">
          <h1>Welcome to {{ project_title }}</h1>
          <p>Your project is ready to go! Start building something amazing with your custom color palette.</p>
          <div className="flex flex-center" style={{ gap: '1rem' }}>
            <button className="btn btn-accent btn-lg">Get Started</button>
            <button className="btn btn-outline btn-lg">Learn More</button>
          </div>
        </div>
      </section>

      {/* Content Section */}
      <section className="section">
        <div className="container">
          <div className="grid grid-3">
            <div className="card">
              <div className="card-header">
                <h3 className="card-title">Fast Setup</h3>
              </div>
              <p>Your project was generated with best practices and is ready for development right away.</p>
            </div>
            <div className="card">
              <div className="card-header">
                <h3 className="card-title">Responsive Design</h3>
              </div>
              <p>Built mobile-first with modern CSS for beautiful, responsive layouts that work everywhere.</p>
            </div>
            <div className="card">
              <div className="card-header">
                <h3 className="card-title">Custom Colors</h3>
              </div>
              <p>Your selected color palette is already integrated and ready to use throughout your app.</p>
            </div>
          </div>

          {/* Interactive Demo */}
          <div className="mt-8 text-center">
            <div className="card" style={{ maxWidth: '500px', margin: '0 auto' }}>
              <h3>Interactive Demo</h3>
              <p className="mb-4">Click the buttons to see your colors in action:</p>
              <div className="flex flex-center" style={{ gap: '1rem', flexWrap: 'wrap' }}>
                <button 
                  onClick={() => setCount((count) => count + 1)}
                  className="btn btn-primary"
                >
                  Primary ({count})
                </button>
                <button 
                  onClick={() => setCount((count) => count + 1)}
                  className="btn btn-secondary"
                >
                  Secondary
                </button>
                <button 
                  onClick={() => setCount((count) => count + 1)}
                  className="btn btn-accent"
                >
                  Accent
                </button>
              </div>
            </div>
          </div>
        </div>
      </section>

      {/* Footer */}
      <footer style={{ backgroundColor: 'var(--color-primary)', color: 'white', padding: '2rem 0', textAlign: 'center' }}>
        <div className="container">
          <p>&copy; 2025 {{ project_title }}. Generated with Bash Stash.</p>
        </div>
      </footer>
    </div>
  )
}

export default App
